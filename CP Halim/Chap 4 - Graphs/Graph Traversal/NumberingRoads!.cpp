//
// Created by Mohi
//

#include <bits/stdc++.h>

using namespace std;

int main() {
    int n, r, cases = 1;
    while(scanf("%d%d", &r, &n), n | r) {
        cout << "Case " << cases++ << ": ";
        if (n * 27 < r) cout << "impossible";
        else {
            int ans = n;
            while(ans < r) {
                ans +=
            }
        }
    }
    return 0;
}