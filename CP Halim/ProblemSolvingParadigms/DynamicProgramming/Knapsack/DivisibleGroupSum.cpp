//
// Created by Student on 13/07/2023.
//

#include <bits/stdc++.h>

using namespace std;

//#define MAX_SIZE 1000;

int arr[1000];

int result[15][15];

long long solve(int arr, int D, int M) {
    for (int i = D; i < )
}

int main() {
    int N, Q, SET = 1, QUERY;
    memset(result, -1, sizeof result);
//    for (int i = 0; i < 25; i++) {
//        for (int k = i; k < 25; k += i) {
//
//        }
//    }
    while(scanf("%d %d", (N | Q))) {
        QUERY = 1;
        for (int i = 0; i < N; i++)
            scanf("%d", &arr[i]);

        int
    }
    return 0;
}