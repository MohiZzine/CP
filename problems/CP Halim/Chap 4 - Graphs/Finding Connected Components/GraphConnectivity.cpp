#include <bits/stdc++.h>

using namespace std;


const int VISITED = 1;
const int UNVISITED = -1;
vector<vector<int>> adjList(26);
vector<int> num_count(26, 0);

int main() {
    int n; cin >> n;
    while(n--) {
        char c; cin >> c;
        char nodes[c - 'A'];
        string line;
        while(getline(cin, l) ) {

        }
    }
    return 0;
}