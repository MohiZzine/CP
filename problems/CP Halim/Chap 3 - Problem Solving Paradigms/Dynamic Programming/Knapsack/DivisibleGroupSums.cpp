#include <bits/stdc++.h>

using namespace std;

int nums[200], dp[200][25];

int main() {
    int n, q;
    while(scanf("%d%d", &n, &q), n | q) {
        for (int i = 0; i < n; i++) cin >> nums[i];

    }
    return 0;
}