#include <bits/stdc++.h>

using namespace std;

int n, k, m;
long long bc(int totalUsed, int numBar, int colorUsed) {
    if (totalUsed == n) return 1;
    if (colorUsed > m || numBar == 0) return 0;

}

int main() {
    cin >> n >> k >> m;

    return 0;
}