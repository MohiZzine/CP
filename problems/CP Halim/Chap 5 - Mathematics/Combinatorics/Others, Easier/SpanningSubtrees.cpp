#include <bits/stdc++.h>

using namespace std;

int n;
const int MAX = 400;

long long f[MAX + 1];

int main() {
    int cases = 1;
    while(scanf("%d", &n), n) {
        printf("Case %d: ", cases++);

    }
    return 0;
}