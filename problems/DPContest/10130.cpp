//
// Created by Student on 13/07/2023.
//

#include <bits/stdc++.h>

using namespace std;

int main() {
    int TC; cin >> TC;
    while(TC--) {
        int N, n_p; cin >> N;
        int w[N], p[N];
        for (int i = 0; i < N; i++)
            cin >> p[i] >> w[i];
        cin >> n_p;
        int G[n_p];
        for (int k = 0; k < n_p; k++) {
            cin >> G[k];
        }


    }

    return 0;
}
