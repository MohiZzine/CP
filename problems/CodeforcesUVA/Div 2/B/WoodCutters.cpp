#include <bits/stdc++.h>

using namespace std;

int main() {
    int n; cin >> n;
    long long coord[n], height[n];
    for (int i = 0; i < n; i++)
        cin >> coord[i] >> height[i];

    
    return 0;
}