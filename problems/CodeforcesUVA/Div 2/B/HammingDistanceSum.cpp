#include <bits/stdc++.h>

using namespace std;

int main() {
    string a, b; cin >> a >> b;
    bitset<200000> bs1(a), bs2(b);


    long long ans = 0;
    for (int i = 0; i <= b.size() - a.size(); i++) {

    }
    return 0;
}