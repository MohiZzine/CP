#include <bits/stdc++.h>

using namespace std;

#define MAX 10000000
int primes[MAX], count = 0;
void fill_primes() {
    for (int i = 2; i < MAX; i++) {

    }
}

int main() {
    int n;
    while(scanf("%d", &n) != EOF) {

    }
    return 0;
}