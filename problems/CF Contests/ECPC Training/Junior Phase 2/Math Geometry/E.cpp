#include <bits/stdc++.h>

using namespace std;

int main() {
    long long n; cin >> n;
    long long k = sqrt(2 * n);
    if (k * (k + 1) / 2 <= n) cout << k;
    else cout << sqrt(2 * )
    return 0;
}