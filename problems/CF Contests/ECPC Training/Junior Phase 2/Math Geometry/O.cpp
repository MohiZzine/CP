#include <bits/stdc++.h>

using namespace std;

int main() {
    char num[10050], sum[10050], prod[10050];
    cin >> num;
    for (int i = 0; i < strlen(num); i++) {
        sum[i] = num[i] +
    }
    return 0;
}