#include <bits/stdc++.h>
#include <algorithm>
using namespace std;

int main() {
  string s, new_s = ""; cin >> s;
  map<int, int> m();

  for (int k = 0; k < s.size(); k += 2) {
    m[s[k]] = 0:
  }
  for (int i = 0; i < s.size(); i += 2) {
    m[s[i]] += 1
  }
  
  return 0;
}