#include<bits/stdc++.h>

using namespace std;

int solve(int arr[], int n, int a, int b) {
  if (a > b) swap(a, b);
  for (int i = 0; i < n; i++) {
    if (arr[i] >= a)
  }
}

int main() {
  int n, q; cin >> n >> q;
  int arr[n];
  while(q--) {

  }
 return 0;
}